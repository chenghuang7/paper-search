# 验证记录

2026-09-06，本地 Python 3.9.6。

- 17 项单元与集成测试通过：关键词规则、多主题去重、修订更新、首次回填、长期中断补抓、配置变更回填、分页异常、有限重试、失败事务、静态构建与嵌入 JSON 转义。
- 完整调用真实 arXiv API，首次收录 129 篇论文；检查全部记录有摘要、作者和匹配主题，ID 唯一。
- 使用数据副本再次执行真实检索：新增 0 篇，总计仍为 129 篇，未产生重复记录。
- JavaScriptCore 语法检查通过；GitHub Checks 另含 `node --check web/app.js`。
- 生成 `dist/index.html`，静态资源相对链接存在，本地 HTTP 返回 200。
- 两份 GitHub Actions 工作流通过 YAML 语法解析。
- 浏览器自动化不可用，电脑控制权限未授予；桌面/手机视觉检查和点击交互验收尚未完成。
- GitHub Actions 和 Pages 工作流已配置，但尚未在用户仓库实际运行。部署需要用户提供目标仓库，并具备该仓库访问权限。

首次抓取是实测结果，不能保证未来每次都成功；故障时保留历史记录和检查点，便于恢复。
